include base-mixins

mixin signIn
    main.mdl-layout__content.mdl-color--grey-100
        +baseButton('elf-sign-in', 'Sign in with Google')

        p#load Firebase SDK Loading…

        p Firebase sign-in status:
            span#elf-sign-in-status Unknown

        <pre><code id="elf-user">null</code></pre>
        <pre><code id="elf-details">null</code></pre>
        <pre><code id="elf-oauthtoken">null</code></pre>
        img#elfPhoto(src='favicon.png', alt='', width="10%", min-width="120px")

mixin userData
    div
        p#userName
        p#userEmail
        p#userPhotoUrl
        p#userId
        p#userImg
    +baseButton('elfShowCurrentUser', 'elfGetCurrentUser')

mixin menuButtons
    +baseButton('pres-toggle', 'Presidents')
    +baseButton('user-toggle', 'Users')
    +baseButton('sign-in-toggle', 'SignIn')

mixin baseButtons
    div
        +baseButton('elfDatabasePush', 'elfDatabasePush')
        +baseButton('elfDatabaseGet', 'elfDatabaseGet')

mixin presidentButtons
    div
        +baseButton('elfInsertFromFile', 'elfInsertFromFile')
        +baseButton('elfUpdatePresident', 'elfUpdatePresident')
        +baseButton('elfPushPresident', 'elfPushPresident')
        +baseButton('elfGetPresidents', 'elfGetPresidents')

mixin serverButtons
    h2 Server Fetch
    div
        +baseButton('serverFetch Server', 'Fetch')

mixin userInput
    h2 User Data
    p Input user data.
    +baseInput('userInput', 'userInput')
    div
        +baseButton('elfInput', 'elfInput')
        +baseButton('elfDatabaseGetAllQux', 'elfDatabaseGetAllQux')
    ul#userInputList

mixin presidentInput
    h2 Presidents
    +baseInput('elfFirstName', 'elfFirstName')
    +baseInput('elfLastName', 'Last Name')
